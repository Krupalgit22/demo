/* =============================================
   OFFCANVAS NAV — TEXT & DROPDOWN COLORS
   ============================================= */

/* Main nav links — white */
.offcanvas.offcanvas-nav .navbar-nav .nav-item .nav-link {
    color: #fff !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: 14px 0;
    font-size: 16px;
}

/* Hover state */
.offcanvas.offcanvas-nav .navbar-nav .nav-item .nav-link:hover {
    color: #ED3538 !important;
}

/* Active state */
.offcanvas.offcanvas-nav .navbar-nav .nav-item .nav-link.active,
.offcanvas.offcanvas-nav .navbar-nav .nav-item.active > .nav-link {
    color: #ED3538 !important;
}

/* Dropdown toggle arrow color */
.offcanvas.offcanvas-nav .navbar-nav .nav-item .nav-link.dropdown-toggle {
    color: #fff !important;
}

/* Dropdown menu background */
.offcanvas.offcanvas-nav .navbar-nav .dropdown-menu {
    background-color: rgb(255, 255, 255) !important;
    border: none !important;
    box-shadow: none !important;
    border-radius: 8px !important;
    padding: 6px 0 !important;
}

/* Dropdown items — white */
/* .offcanvas.offcanvas-nav .navbar-nav .dropdown-menu .dropdown-item {
    color: rgba(255, 255, 255, 0.85) !important;
    font-size: 15px;
    padding: 10px 20px !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
} */
.offcanvas.offcanvas-nav .navbar-nav .dropdown-menu .dropdown-item {
    color: rgb(0 0 0 / 85%) !important;
    font-size: 15px;
    padding: 10px 20px !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}
/* Dropdown item hover */
.offcanvas.offcanvas-nav .navbar-nav .dropdown-menu .dropdown-item:hover {
    color: #ED3538 !important;
    background-color: rgba(255, 255, 255, 0.08) !important;
    padding-left: 26px !important;
}

/* Dropdown item active */
.offcanvas.offcanvas-nav .navbar-nav .dropdown-menu .dropdown-item.active,
.offcanvas.offcanvas-nav .navbar-nav .dropdown-menu .dropdown-item:active {
    color: #ED3538 !important;
    background-color: rgba(237, 53, 56, 0.1) !important;
}

/* Dropdown arrow icon color fix */
.offcanvas.offcanvas-nav .navbar-nav .nav-link.dropdown-toggle::after {
    color: #fff;
    border-top-color: #fff;
}

/* Offcanvas header logo area */
.offcanvas.offcanvas-nav .offcanvas-header {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding-bottom: 16px;
    margin-bottom: 8px;
}

/* Close button white */
.offcanvas.offcanvas-nav .btn-close {
    filter: invert(1);
    opacity: 0.8;
}

.offcanvas.offcanvas-nav .btn-close:hover {
    opacity: 1;
}